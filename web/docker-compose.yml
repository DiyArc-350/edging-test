version: '3.8'

services:
  gallery-web:
    image: nginx:alpine
    container_name: gallery_nginx
    ports:
      - "8082:80"
    volumes:
      # We map the generated HTML files to Nginx's default serving directory
      - ./html_files:/usr/share/nginx/html:ro
    restart: unless-stopped
